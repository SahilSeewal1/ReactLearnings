const PropStateChange2 = props => {
    const {name, shortName} = props
    
    return ( <div>
        <h1>Hello!! '{name}' short for '{shortName}'</h1>
        </div>)
    
     }

    export default PropStateChange2